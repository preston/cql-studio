(function(window) {
    // Environment variables
    window["CQL_WORKBENCH_RUNNER_BASE_URL"] = "${CQL_WORKBENCH_RUNNER_BASE_URL}";
    window["CQL_WORKBENCH_FHIR_BASE_URL"] = "${CQL_WORKBENCH_FHIR_BASE_URL}";
    window["CQL_WORKBENCH_TRANSLATION_BASE_URL"] = "${CQL_WORKBENCH_TRANSLATION_BASE_URL}";
})(this);